/** const autoprefixer = require("autoprefixer");
const { postcss } = require("tailwindcss"); */

/** module.exports = {
  style: {
    postcss: {
      plugins: [require("tailwindcss"), require("autoprefixer")],
    },
  },
}; */

/** @type {import('@craco').Config} */

/** 
const style = {
  postcss: {
    plugins: [require("tailwindcss"), require("autoprefixer")],
  },
};

*/
